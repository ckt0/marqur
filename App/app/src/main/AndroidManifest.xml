<?xml version="1.0" encoding="utf-8"?>

<!-- The Marqur Manifest -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.marqur.android">

<!--    App Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

<!--    App-Level Settings -->
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Marqur"
        android:fullBackupContent="@xml/backup_descriptor">

<!--        Keys and Stuff -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

<!--        List of Activities -->

<!--        Startup/Overall Activity -->
        <activity android:name="com.marqur.android.MainActivity"
		    android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
               <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

<!--        SignUp Activity -->
        <activity android:name="com.marqur.android.SignupActivity"
            android:label="@string/title_activity_signup" />

<!--        Login Activity -->
        <activity android:name="com.marqur.android.LoginActivity"
            android:label="@string/title_activity_login" />

<!--        Maps Activity (Loads in Map fragment) -->
        <activity android:name="com.marqur.android.MapsActivity"
            android:label="@string/title_activity_map_view" />

<!--        AddMarker Activity -->
        <activity android:name="com.marqur.android.AddMarker"
            android:label="@string/title_activity_map_add_marker" />

    </application>

</manifest>